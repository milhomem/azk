# Azkfile.js

Aqui vem uma descrição sobre o markdown

```js
system({
  'docs': {
    depends: [],
    http: {
      hostname: "#{system.name}.azk.#{azk.default_domain}",
    }
  }
})
```

## Systems
